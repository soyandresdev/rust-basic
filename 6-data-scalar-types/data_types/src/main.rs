fn main() {
    println!("Hello, world!");
    // Data types

    // Scalar Types
    /*
        Number
            i8 => Number 8 bit
            i16 => Number 16 bit
            i32 => Number 128 bit
            i64 => Number 64 bit
            i128 => Number 128 bit
            This can save negative and positive'
            If you want only save positive use there: 
                u8 => Number 8 bit
                u16 => Number 16 bit
                u32 => Number 128 bit
                u64 => Number 64 bit
                u128 => Number 128 bit
    */
    let _number: i32 = 120;

    /*
        Float
        f32 => Number 32 bit
        f64 => Number 64 bit
    */
    let _x = 2.0; // f64

    let _y: f32 = 3.0; // f32

    /*
        chart
        '' -> UTF-8
    */
    let _c = 'z';
    let _z = 'ℤ';
    let _heart_eyed_cat = '😻';

    /*
        Boolean
        false
        true
    */
    let _t = true;
    let _f: bool = false; // with explicit type annotation

}
